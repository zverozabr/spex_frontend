const PathNames = {
  projects: 'projects',
  jobs: 'jobs',
  tasks: 'tasks',
  resources: 'resources',
  pipelines: 'pipelines',
  visualization: 'visualization',
  dataset: 'dataset',
  img: 'img',
};

export default PathNames;
