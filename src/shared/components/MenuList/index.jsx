import MenuItemOrigin from '@material-ui/core/MenuItem';
import MenuListOrigin from '@material-ui/core/MenuList';
import styled from 'styled-components';

const MenuItem = styled(MenuItemOrigin)`

`;

const MenuList = styled(MenuListOrigin)`

`;

export {
  MenuList as default,
  MenuItem,
};

